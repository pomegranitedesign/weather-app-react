const _initialState = {
  isWeatherFetching: true,
  weatherData: [],
  cityName: '',
  countryName: '',
}

export default (state = _initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
